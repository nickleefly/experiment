// g++ -v -o test test.cpp
#include <iostream>
#define foreach for (int
#define in
#define range(x, y) = x; i < y; i++)
#define print(x) std::cout << x;
#define fn int
#define does {
#define done }

fn main () does

    foreach i in range(0, 4)
        print("hello\n")

done